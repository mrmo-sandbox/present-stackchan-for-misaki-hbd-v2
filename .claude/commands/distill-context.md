---
description: Distill raw material from docs/context into an agreements PR.
---

Read and follow `.github/prompts/distill-context.prompt.md`, using `$ARGUMENTS`
as its `${input:topic}` (the topic directory under `docs/context/`).
