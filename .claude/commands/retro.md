---
description: Mine recent PRs, reviews, and failed runs for failure patterns; open a retro PR.
---

Read and follow `.github/prompts/retro.prompt.md`, using `$ARGUMENTS`
as its `${input:lookback}` (the lookback window, e.g. "last 20 merged PRs").
