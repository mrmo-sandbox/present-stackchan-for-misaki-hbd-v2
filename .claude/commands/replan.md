---
description: Propagate a task outcome or deviation through the plan.
---

Read and follow `.github/prompts/replan.prompt.md`, using `$ARGUMENTS`
as its `${input:issueNumber}` (the trigger issue number).
