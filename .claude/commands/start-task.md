---
description: Start work on a Task issue following the child-session start ritual.
---

Read and follow `.github/prompts/start-task.prompt.md`, using `$ARGUMENTS`
as its `${input:issueNumber}` (the Task issue number).
