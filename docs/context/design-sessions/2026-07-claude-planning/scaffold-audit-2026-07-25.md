---
source: Claude planning session (this repo's kickoff/audit conversation)
retrieved: 2026-07-25
method: export
collector: Claude (Fable 5) planning session
sensitivity: internal
status: raw
---

# Audit: Can the tt1 scaffold run the Claude-plans / Codex-implements flow today?

Repo: mrmo-sandbox/present-stackchan-for-misaki-hbd-v2 (local clone at /Users/takashi.kawamoto/Claude/Projects/pj-presetn-stackchan-for-misaki-hbd). Audit date 2026-07-25. All evidence is file:line in the local clone or live `gh` read-only output, as reported by six repo-area readers, three research reports, and five revision-pass re-verifications; disagreements between sources are flagged inline.

## 1. Verdict

The core machinery is real and mostly surface-neutral: Epics #2–#10 exist with native blocked-by wiring, gh 2.96.0 satisfies new-task.sh's >=2.94 requirement, the atomic sub-issue creation path works, all required labels exist, board #5 is live with correct Start/Target dates on all 9 Epics, and AGENTS.md is auto-loaded by both Claude Code (via the CLAUDE.md @import shim, the official Anthropic pattern) and the Codex desktop app (native AGENTS.md support) — so the constitution and the issue-graph protocol reach **Claude Code and Codex** today. One named planning surface is the exception: E0's own criterion names "Claude Desktop (Fable 5)" as the planner (E0-scaffold-and-distill.md:8), and Claude Desktop has no repo-file auto-load mechanism at all — every Desktop session starts constitution-blind (F16). The flow can start, but four things must change first, in order: (1) **main CI is red right now** (scaffold-self-check fails because check-md-links extracts a path-like substring from raw-context material in docs/context/plan-intake/stackchan-plan/HANDOFF.md:49-50 — every future PR inherits this failure, and the failure class will re-fire on any raw-context commit; the fix is a one-line scan-scope exclusion that restores green CI by itself, see F1); (2) **the routing layer still teaches Copilot**: starting with the constitution's own preamble (AGENTS.md:3-5 enumerates the fleet as Copilot surfaces — the first paragraph both target tools read), then task-routing/SKILL.md:28-33 and its frontmatter (:3), the plan-management cookbook example, the canonical task-body template (templates/task-body.md:44,55), the ai-task.yml Verification-field description (:74-76), verification/SKILL.md's routed-environment rule (:70-71), the exec:* descriptions hardcoded in scripts/setup-labels.sh:23-26 (and mirrored live), plus the orchestrator role definition (and the Copilot-client header comment in all three .agent.md files), the session-orchestration frontmatter, the epic template form blurb, code-review.instructions.md:7, and the ci.yml header all route implementation to Copilot surfaces — and the distill-context prompt additionally scripts an outright issue-less PR path (F12) — while the Claude/Codex reinterpretation exists only as prose in Epic #2 / E0's raw-context md — any stateless session obeying the §9 start ritual will route implementation to Copilot; (3) **the cross-tool dispatch mechanics are undocumented on every lane** — not only how a frontier task reaches a Codex desktop thread (gh auth + network egress prerequisites, steering channel), but also how the iron rule's "dedicated fresh session per task" is started on the Claude side (the per-task-worktree step exists only inside the Copilot-format start-task prompt neither target tool discovers), and the Claude Desktop bootstrap if Desktop stays a planning surface; (4) **onboarding has not run** (tuning-status.sh: NOT TUNED, 7 findings — 6 CUSTOMIZE markers + 1 placeholder sentinel at copilot-setup-steps.yml:33; the `quality` CI job is a no-op; ruleset 19131818 is disabled and — with required_approving_review_count=1, require_code_owner_review=false, no bypass actors — would deadlock a solo-human repo if enabled as-is). None of these is architectural: (1) is a one-line scan-scope fix, (2)–(4) are exactly what E0's own phase outline (E0-scaffold-and-distill.md:19-26, criterion at line 8) already schedules — the fix is to cut E0 into the sub-issues in §4, opening with a small T-E0-0 hygiene task so the reformed process's first act is itself an instance of the process.

## 2. Gap table

| Req | Status | Evidence | Gap |
|---|---|---|---|
| R1 Epic outline drives work | **partial** | `gh issue list --label type:epic` → 9 open issues #2–#10, created 2026-07-18; `gh api graphql`/`gh issue view --json blockedBy` confirms wiring (#3←#2, #4←#3, #5←#2, #6←#4,#5); board #5 live-verified: `gh project view 5 --owner mrmo-sandbox` → "present-stackchan-for-misaki-hbd-v2 roadmap", 9 items; `gh project item-list 5` → all 9 Epics carry Start/Target dates exactly matching HANDOFF.md:29-41 (#2 = 2026-07-20/2026-08-31 … #10 = 2027-06-01/2027-07-25), Kind=Epic set | Issues, deps, board membership, and dates all verified live. Only the Roadmap *view*'s existence is CLI-unverifiable (view config is not exposed by the API — setup-project.sh:44-47); confirm once in the UI. Board Status-field semantics/automation (auto-Done on close etc.) are undefined anywhere — fold a one-line decision into the T-E0-1b ADR. Epic titles are Japanese, violating AGENTS.md §8 (:64-68). blocked-by is informational only — GitHub never enforces it (github.blog 2025-08-21 changelog); frontier.sh is the sole gate. |
| R2 Task sub-issues, JIT, deps + ai:ready + one exec:* | **partial** | new-task.sh:41-59 (atomic `gh issue create --parent --blocked-by`, label whitelist); `gh --version` 2.96.0 ≥ 2.94; `gh label list` shows the full 10-label canonical set; breakdown-epic.prompt.md:6-26; frontier.sh JSON parsing verified against live issue #3 | Mechanics fully work from Claude Code. Gaps: prompts not discoverable in Claude Code (.claude/commands/ absent — .claude/ holds only untracked settings.local.json); plan-management/SKILL.md:70 example hardcodes `exec:cloud` (the label declared unused) and the canonical task-body template every new task copies (new-task.sh:9-10) still enumerates the Copilot surface set including exec:cloud (templates/task-body.md:44,55 — see F2); no exec:* slug is defined for Claude-executed planning tasks under the target semantics (see F2/Q5); label-set integrity is unguarded: live `gh label list` shows 11 non-canonical labels alongside the 10 canonical (9 GitHub defaults + dependabot's `dependencies`/`github_actions`), setup-labels.sh only creates/refreshes (never prunes), no CI check validates labels, and the raw `gh issue create --parent` path breakdown-epic.prompt.md permits bypasses new-task.sh's whitelist (F2); new-task.sh does not chain into `setup-project.sh dates`, and breakdown-epic.prompt.md step 5 (:22-26) omits the board-dates step that plan-management/SKILL.md:105-113 mandates — so a prompt-following session will skip it (F12); frontier.sh output lacks exec:* labels (frontier.sh:65-68) and has no type:task filter. |
| R3 Issue-first + session-per-task codified for both tools | **partial** | AGENTS.md §2-§4, §9 (:14-41,70-76); session-orchestration/SKILL.md:15-24 (1:1:1:1), :56-66 (Outcome comment format), :79-84 ("No issue, no work") | Ritual is codified and surface-neutral (pure gh CLI). But Codex auto-loads ONLY AGENTS.md (research: no @-import mechanism; Codex docs verified at developers.openai.com/codex, which 308-redirects to learn.chatgpt.com — both hosts serve the same pages) — copilot-instructions.md and all skills reach Codex only via in-text §9 compliance; §4's accepted branch equivalent is a "tool-generated prefix (e.g. `<user>-task-<n>-<slug>`)" (AGENTS.md:36-41), i.e. variants that still embed the issue number, and session-orchestration's mapping table hardcodes the same (:18-19) — Codex-generated branch names typically carry a slug but no issue number, so accommodation is plausible but unpinned (a literal-minded session would flag every Codex branch as a §4 deviation); worse, distill-context.prompt.md:18 scripts an issue-less PR path outright ("branch `task/<issue-or-topic-slug>`" — F12); the dispatch step is documented on NO lane — neither Claude→Codex nor the Claude-side session-per-task mechanic (F3); Codex sandbox network policy (gh calls prompt/fail by default) is an unstated prerequisite for record-before-report. |
| R4 Rolling-wave replanning | **partial** | plan-management/SKILL.md:30-40 (JIT), :105-113 (board dates at creation AND "update it whenever replanning moves the schedule", via `setup-project.sh dates`), :121-142 (6-step replanning), :144-155 (anti-patterns); setup-project.sh cmd_dates (:189-289) scripts idempotent item-add + both date fields + Kind; replan.prompt.md:6-23; retro/SKILL.md loop | Procedures exist, are scripted, and are executable from Claude Code; gh version precondition met. Gaps: /replan not a Claude Code slash command; replan.prompt.md's checklist does not repeat the SKILL's board-dates step (grep: no dates/project mention), so board sync depends on the session reading the full SKILL, not just the prompt (F12). |
| R5 Claude-as-planner supported | **partial** | CLAUDE.md:1-5 = `@AGENTS.md` + `@.github/copilot-instructions.md` — exactly the official Anthropic bridge (code.claude.com/docs/en/memory); agent/prompt bodies are tool-agnostic gh + repo paths | Nothing hard-blocks Claude **Code**. Claude **Desktop** — the surface E0 actually names — has no delivery path for any of this (F16). Friction on Code: .claude/settings.local.json is untracked and allowlists only `gh --version`/`gh auth *`, so every `gh issue`/`gh pr` call prompts, on every clone/worktree; worse, this clone's .git/info/exclude:7 contains `.claude/`, which silently blocks committing any .claude/ fix (F8); no .claude/commands, .claude/agents, .claude/skills, or root .mcp.json exist; roles adopted by convention (Read + act-as) only. |
| R6 Codex-desktop-as-implementer supported | **partial** (routing layer: missing) | Research (high confidence): Codex natively reads root AGENTS.md, has built-in per-thread worktrees matching 1-task-1-branch, scans `.agents/skills` NOT `.github/skills` (developers.openai.com/codex/skills, verified live), ignores `.vscode/mcp.json` and `.github/prompts/`; task-routing/SKILL.md:28-33 defines exec:app as "Copilot app session"; setup-labels.sh:23-26 hardcodes the same wording; E0 reinterpretation only at E0-scaffold-and-distill.md:8 and HANDOFF.md:62 | AGENTS.md delivery works (accidental advantage — though its own preamble is Copilot-worded, F2i). Everything else is missing: exec:app has no durable Codex meaning; start-task.prompt.md (the implementer ritual) is not deliverable to Codex; skills invisible to Codex's native discovery; no MCP config Codex reads (harmless — flow is gh-driven); gh auth + network egress in the Codex sandbox unverified/undocumented; reviewer role assigned to no surface. |
| R7 Onboarding debt visible and scheduled | **partial** | `bash scripts/tuning-status.sh` exit 1, 7 findings — 6 CUSTOMIZE markers (copilot-instructions.md:14,36; ci.yml:24; copilot-setup-steps.yml:22; firmware.instructions.md:7,33) + 1 placeholder sentinel (copilot-setup-steps.yml:33, matched by the dedicated grep at tuning-status.sh:51); E0-scaffold-and-distill.md:8,19-26 schedules routing reinterpretation + verified commands; `gh api rulesets` → id 19131818 enforcement=disabled; retro-hygiene.yml (cron `0 0 1 * *`, first fire 2026-08-01) auto-files a monthly needs:human hygiene issue and reports the ~150-line always-on budget (retro-hygiene.sh:44; AGENTS.md currently 96 lines, copilot-instructions.md 86) — surface-neutral, needs no reinterpretation | Debt is enumerated and E0 owns it. But: main CI is red from an UNSCHEDULED defect (run 29627227902, check-md-links MISSING scripts/create-epics.sh); the disabled ruleset requires 1 approving review with no code-owner requirement and no bypass actors, which deadlocks the solo-human + same-account-agent setup if enabled unmodified; copilot-setup-steps.yml (2 of the 7 tuning findings) serves a surface the model never uses — fate undecided (deleting it clears its 2 findings with no tuning-status.sh edit — tuning-status.sh:45 skips missing TARGETS files — but the other 5 CUSTOMIZE markers still block TUNED either way, and see F5 for the doc-ref ripple); retro-hygiene will file its first automated issue within a week of this audit with no assigned triage lane (F14). |
| R8 Guardrails coherent for mixed-tool fleet | **partial** | PULL_REQUEST_TEMPLATE.md:1-38 fully tool-agnostic; CODEOWNERS:3-11; code-review.instructions.md:5-31 targets agent and human reviewers; AGENTS.md §5, §8; scripts/check-template-sync.sh (run by scaffold-self-check, ci.yml:75-76) is a working, tool-agnostic guardrail keeping issue-form `label:` values in ordered sync with the canonical body templates (check_pair calls :120-123 covering ai-task.yml↔task-body.md and epic.yml↔epic-body.md); verification layer 2 substantially live, verified: CodeQL default setup green (`gh run list`: "CodeQL Setup" success 29627230231, scheduled scans 30066596850/30066595646 success 2026-07-24); Dependabot version updates active: PR #1 open (created 2026-07-18, all 4 checks SUCCESS — note these ran 21s BEFORE the CI-breaking commit 35c1f6f landed on main; a rebase or re-run would hit the F1 failure, consistent with the verdict, so the green is stale, not contradictory) + successful version-update scan runs (latest 30135720126, 2026-07-25); `gh api repos/.../security_and_analysis` (fifth pass): secret_scanning=enabled, secret_scanning_push_protection=enabled, secret_scanning_non_provider_patterns=enabled, secret_scanning_validity_checks=enabled | PR template works verbatim for Codex PRs, but nothing validates it in CI; CODEOWNERS review is unenforced both now (ruleset disabled) AND after enabling as-is, because ruleset 19131818 sets require_code_owner_review=false (gh api output; also hardcoded in setup-ruleset.sh:~99); no CI check that diff ⊆ File-ownership (pure review discipline, and Copilot code review — verification/SKILL.md:21-23 — is out of the loop with no replacement); the two genuinely open security items (verified, no longer "unchecked"): dependabot_security_updates=disabled, and no dependency-review workflow exists — both are T-E0-3 decisions; §8 English-only is actively violated by 9 Japanese epic titles, the committed Japanese plan-intake bundle, and the Japanese root README.md; path-scoped `applyTo` instructions (e.g. docs/agreements PR-only rule) auto-inject on NO surface in the target model. |

## 3. Findings detail

**F1 — Main CI is red (blocker for every future PR), and the failure class will recur.** scaffold-self-check fails: check-md-links extracts `scripts/create-epics.sh` from docs/context/plan-intake/stackchan-plan/HANDOFF.md:49-50 (unanchored REF_PATTERN at scripts/check-md-links.sh:24 matches the substring mid-path) and resolves it repo-root-relative where it does not exist; the file actually lives at docs/context/plan-intake/stackchan-plan/scripts/create-epics.sh. Verified live (run 29627227902) and reproduced locally. CODE-KICKOFF.md:72,80 already uses the full path and passes. *Why it matters:* every Codex PR will show a failing required-context check; verify-before-done becomes noise. And the root cause is a class, not an instance: the scan covers ALL tracked md (check-md-links.sh:32), including docs/context raw material, and T-E0-6 will commit conversation logs and research notes (HANDOFF.md:55) that will inevitably contain path-like substrings. *Fix:* a single-line scan-scope exclusion — `git ls-files '*.md' ':!docs/context/**'` at check-md-links.sh:32 — fixes BOTH the current red (the only failing refs come from HANDOFF.md:49-50, verified — but see the allowlist caveat below for why that is so) and the recurrence class in one edit, and restores green CI by itself: in run 29627227902 the md-links step is the ONLY failing step — shellcheck, actionlint, and check-template-sync all executed and passed (verified via `gh run view --json jobs`). Do NOT rewrite the HANDOFF.md refs: the exclusion makes that edit redundant, and touching committed raw-context content cuts against docs.instructions.md:17-19 ("intake area … do not clean up raw material") — note HANDOFF.md:56 declares only the epics/*.md payload obsolete post-filing, not the whole bundle, so the raw-material rule, not that line, is what protects HANDOFF.md itself. Do NOT use ALLOW_PREFIXES either: the extracted ref is the root-relative `scripts/create-epics.sh`, so allowlisting it would permanently mask a genuinely missing root-level script. ALLOW_PREFIXES caveat (corrected from an earlier draft): the two shipped entries (check-md-links.sh:26-29) are NOT both dead config. The `docs/context/device-pairing/` entry is load-bearing today — the checker's own header (check-md-links.sh:11) says it is "cited in docs/agreements/*", and verified: docs/agreements/requirements.md:10 references docs/context/device-pairing/interview-2026-07-01.md and docs/agreements/non-goals.md:7 references docs/context/device-pairing/INDEX.md — both REF_PATTERN-matching (.md extension), both nonexistent, both in files that REMAIN SCANNED after the `:!docs/context/**` exclusion (the exclusion narrows which files are scanned, not which targets may be referenced). Deleting that entry would re-break the very check T-E0-0 exists to fix, with 2 missing refs; it must stay until T-E0-7 replaces the "(example — replace)" rows in docs/agreements/{requirements,non-goals}.md — files T-E0-7 owns — and T-E0-7's brief removes the allowlist entry in that same PR. Corollary: the "only failing refs come from HANDOFF.md" claim above is true only BECAUSE the allowlist masks the device-pairing refs (the OK path reports them as "allowlisted example reference(s) skipped"). Only `docs/context/line-integration/` is genuinely deletable now: its sole citation (context-collection/SKILL.md:18) is a bare directory name with no file extension, which REF_PATTERN ignores — delete that one entry in T-E0-0's diff, keep device-pairing. Scanner-consistency caveat (prospective only): scaffold-self-check also runs `git ls-files -z '*.sh' | xargs -0 shellcheck -S style` over ALL tracked shell scripts (ci.yml:61-62) — and that step runs BEFORE the md-links step (ci.yml:78-79), executed in the failing run, and passed: create-epics.sh passes `shellcheck -S style` today, verified locally with the CI-pinned 0.11.0. So no shellcheck failure is currently masked; the exposure is that FUTURE raw-context .sh commits from T-E0-6 could fail at strict `style` severity. Decide once, scanner-consistently: either exclude docs/context/** from the shellcheck file list too, or state in T-E0-6's brief that committed raw-context .sh files must pass `shellcheck -S style`. **[T-E0-0 + class fix; device-pairing allowlist removal rides T-E0-7; the shellcheck-scope decision rides T-E0-3, which owns ci.yml — see §4]**

**F2 — Routing layer still teaches Copilot; the Claude/Codex reinterpretation is not durable.** Direct Copilot-teaching artifacts (verified inventory, wider than first reported): (i) — added in the fourth pass, and the single highest-visibility site — AGENTS.md:3-5, the constitution's own preamble, defines the fleet as "every surface: GitHub Copilot cloud (coding) agent, GitHub Copilot app sessions, Copilot CLI, IDE chat agents, and third-party agents"; since AGENTS.md is the ONLY file Codex auto-loads (R6), the first paragraph both target tools read enumerates Copilot surfaces as the operating fleet (the "and third-party agents" catch-all keeps it from being flow-breaking, but it is stale routing text nonetheless; constitution edits go via retro PR, so the reword rides T-E0-5, not T-E0-1) — same class: CLAUDE.md:1-3's comment ("…so Claude Code behaves identically to the Copilot surfaces. Other agents: read AGENTS.md directly."); (a) task-routing/SKILL.md:28-33 — the Surface matrix (exec:cloud="Copilot cloud agent", exec:app="Copilot app session", exec:cli="Copilot CLI", exec:ide="VS Code + Copilot Chat") — plus its frontmatter description (:3), which ends "…which tool (cloud agent, Copilot app, CLI, IDE) should handle a piece of work" and is exactly what skill-discovery mechanisms surface first; (b) plan-management/SKILL.md:70 cookbook example labels `exec:cloud`; (b2) the canonical body template every new task copies — new-task.sh --help (:9-10) orders every task body to "start from ../templates/task-body.md" — still carries the old vocabulary: templates/task-body.md:55 enumerates "Surface: exec:cloud | exec:app | exec:cli | exec:ide" (including the declared-unused exec:cloud) and :44 says "(exec:cloud tasks get no hardware)", so every future Task issue re-imports stale routing guidance; (b3) verification/SKILL.md:70-71 — the "Writing good Verification sections" rule repeats the same vocabulary ("Commands must run in the task's routed environment (`exec:cloud` tasks get no hardware — split HIL criteria into an `exec:ide` follow-up)"), planner-facing text that would survive a matrix-only rewrite; (b4) ai-task.yml:74-76 — the web issue form's Verification textarea description carries a FOURTH copy of the same parenthetical ("executable on the routed surface (exec:cloud tasks get no hardware)"), and its Surface dropdown lists exec:cloud, the declared-unused lane, as the first selectable option (:86-90) — an earlier draft cleared this file on a grep for the literal string "Copilot", which tested the wrong thing; (c) scripts/setup-labels.sh:23-26 — the CANONICAL source of the label descriptions ("Route: Copilot cloud (coding) agent", "Route: Copilot app session", "Route: Copilot CLI"), idempotently reapplied by `gh label create --force` (:4,:16), mirrored in the live labels (three of four Copilot-worded; exec:ide's "Route: IDE with human in the loop" is already tool-neutral); (d) orchestrator.agent.md — the exact role Claude runs in the target model — whose description (:3) and Dispatch step (:28-32: "spawn a child session, hand it to the cloud agent, or queue it for a human/IDE") route implementation to Copilot surfaces; additionally ALL THREE .agent.md files carry the header comment "verified the exact identifiers supported by your Copilot client version" at :4-5; (e) session-orchestration/SKILL.md:3 frontmatter ("the GitHub Copilot app's session tree … dispatching cloud-agent runs"); (f) epic.yml:13-15's "via the /breakdown-epic prompt" blurb — note this is a `type: markdown` form element, shown in the web form UI when filing an Epic but never included in the submitted issue body, so it reaches humans at filing time only (lower weight, still reword); (g) ci.yml:2-3 ("the cloud agent iterates on their failures"); (h) code-review.instructions.md:7 opens "These standards guide Copilot code review, agent reviewers … and human reviewers alike" — its first named reviewer is the surface F10 says is out of the loop. Partial mitigation on ai-task.yml: the Surface dropdown's options are bare label slugs and its description already delegates to the routing skill ("See .github/skills/task-routing/SKILL.md. Mirror as the exec:* label."), so slug REINTERPRETATION requires no dropdown rewrite — which is exactly why reinterpretation is cheap — but the file is NOT change-free: the :74-76 description and the exec:cloud option (:87) both need the same one-line treatment as the other vocabulary mirrors (see fix). Related label-hygiene gap: nothing guards label-set integrity — live `gh label list` shows 11 non-canonical labels alongside the 10 canonical (the 9 GitHub defaults plus dependabot's `dependencies`/`github_actions`, the latter two legitimate per F14's lane); setup-labels.sh only creates/refreshes, never prunes; no CI check plays the role check-template-sync.sh plays for templates; and the raw `gh issue create --parent` path that breakdown-epic.prompt.md step 5 explicitly permits bypasses new-task.sh's whitelist, so a session can attach default labels or break the exactly-one-exec:* invariant with no tripwire. The target semantics (design=Claude planner/orchestrator, implementation=Codex desktop=exec:app, hardware=exec:ide, exec:cloud unused) exist only at E0-scaffold-and-distill.md:8 / HANDOFF.md:62 / Epic #2 body — raw context, not operative instruction — and they define NO slug for Claude-executed planning/docs tasks, though every Task issue must carry exactly one exec:* label (see Q5). *Why it matters:* AGENTS.md §9 sends every fresh session to task-routing/SKILL.md, which routes implementation to Copilot; this is the single biggest contradiction with the target model. *Fix:* one lean PR (E0's own criterion demands it): rewrite the task-routing matrix rows AND frontmatter keeping the label slugs (reinterpretation beats renaming — new-task.sh:41's whitelist and the dropdown option slugs stay untouched if slugs persist), including an explicit Claude lane (proposal: exec:cli = "Claude Code session — planning/docs/scripted repo work"); fix plan-management/SKILL.md:70 to the reinterpreted slug; update templates/task-body.md:44,55, verification/SKILL.md:70-71, AND ai-task.yml:74-76 (all four copies of the hardware parenthetical) to the new surface vocabulary (e.g. "exec:app Codex sessions have no hardware — split HIL criteria into an exec:ide follow-up"), plus drop or annotate ai-task.yml's exec:cloud dropdown option as reserved/unused — noting that scripts/check-template-sync.sh (run by scaffold-self-check) requires issue-form `label:` values to stay in ordered sync with the templates' `## ` headings, so any change to form section labels needs the paired template heading change in the same PR (the task-body :44/:55 edits are inside comment/body text, not headings; the ai-task.yml edits touch `description:` and `options:` values, not `label:` keys; and the epic.yml blurb edit touches a `type: markdown` value block — all sync-safe); rewrite the three Copilot-worded description strings IN setup-labels.sh (editing only the live labels via `gh label edit` would be silently reverted by any future rerun of the idempotent script — setup-labels.sh:4,16; project-onboarding P4 conditionally invokes it "if labels are missing", SKILL.md:80), with the label-propagating rerun handled as a recorded post-merge follow-up per the mechanism in T-E0-1 (see §4) so live label descriptions never describe semantics that exist on no merged branch; optional low-cost hardening at the same rerun: delete the 9 unused GitHub default labels (shrinks the mislabeling surface) or add one plan-management sentence stating only setup-labels.sh labels are valid on type:epic/type:task issues; reword orchestrator.agent.md's description/Dispatch step, the three agent-file header comments (:4-5), epic.yml's form blurb, ci.yml's header comment, and code-review.instructions.md:7 (all trivial); the AGENTS.md:3-5 preamble and the CLAUDE.md comment reword ride T-E0-5 (constitution changes via retro PR only). The division-of-labor ADR ships as its own separate small Task (T-E0-1b) — docs.instructions.md:20-25 forbids bundling docs/agreements changes into other work, and §4 forbids two PRs from one session, so a separate task, not a second PR from the same session (see §4). **[E0-task]**

**F3 — The dispatch mechanic is undocumented on EVERY lane, not just Claude→Codex.** No skill or prompt says how a frontier task reaches a work session on any surface. (a) *Claude→Codex:* session-orchestration/SKILL.md:88-89 assumes an in-app steering channel; start-task.prompt.md (the implementer start ritual) is VS Code Copilot prompt-file format (`mode: agent`, `${input:...}`) that Codex never discovers (research: Codex custom prompts live in ~/.codex/prompts and are deprecated in favor of skills — developers.openai.com/codex docs). The de-facto transport — human pastes the issue number into a new Codex thread — is nowhere written; steering-by-issue-comment is not specified. Also unstated: Codex needs installed+authenticated gh and network egress (workspace-write sandbox disables network by default; gh calls prompt or fail — developers.openai.com/codex permissions docs), or record-before-report degrades to human relay. (b) *Claude→Claude — under §4's own task cut the FIRST lane exercised, since all nine proposed E0 tasks are exec:cli:* the iron rule ("every Task issue gets its own dedicated fresh work session"; session-orchestration's 1:1:1:1 mapping and its "separate worktree per concurrent child" mandate) has no discoverable mechanic on the Claude side either — the worktree-creation step is documented in exactly one place, start-task.prompt.md:18 ("Create branch task/<n>-<short-slug> (in a fresh worktree if other tasks run in parallel)"), i.e. the child session creates it as part of the start ritual, but that prompt is Copilot-format and neither target tool discovers it (F4), and no skill or script covers the step; nothing discoverable says how a dedicated fresh Claude Code session per task is started or how the child start ritual is invoked in Claude Code — so F4's wrapper list must include a start-task wrapper (the single most-invoked prompt in the whole flow). The session-tree/"message the parent" transport in session-orchestration is app-local on BOTH sides. *Why it matters:* this is the seam the whole division of labor runs across; today it is improvised, which retro/SKILL.md itself predicts becomes a repeat failure. *Fix:* add a surface-agnostic "Cross-tool dispatch" section to session-orchestration/SKILL.md (or a new skill) covering all three lanes: **exec:cli** (Claude Code: human opens a fresh session — or `claude` CLI in a new worktree created with `git worktree add ../wt-task-<n> -b task/<n>-<slug>` — and invokes the start ritual; parent↔child messaging = issue comments, same as Codex); **exec:app** (Codex: parent hands the issue number + a fixed kickoff snippet — "Read AGENTS.md §9, then run the child protocol in .github/skills/session-orchestration/SKILL.md for issue #N; create or rename your branch to task/<n>-<slug>"; steering = comments on the issue that the child re-reads; prerequisites = `gh auth status` green inside a Codex session, network approval or allowlist for github.com/api.github.com); **Claude Desktop bootstrap** if Desktop remains a planning surface (F16). The explicit branch-naming line matters because Codex-generated branch names typically lack the issue number, which §4's accepted-equivalent wording (AGENTS.md:36-41) arguably does not cover — the durable fix is T-E0-5's constitution amendment widening §4's wording (an ADR cannot amend AGENTS.md — see §4 T-E0-5); the kickoff-snippet instruction can stay as belt-and-braces. **[E0-task]**

**F4 — Skills/prompts/agents have zero native discovery on either target tool.** Research (high confidence, official docs): Claude Code scans .claude/skills and .claude/commands; Codex scans .agents/skills (developers.openai.com/codex/skills, verified live); only Copilot scans .github/skills. The repo has none of the mirrors (.claude/ contains only untracked settings.local.json; no .agents/, no root .mcp.json). Prompt files use Copilot-only `${input:...}` syntax; agent files use Copilot custom-agent location. Degrades gracefully today only because AGENTS.md §9 textually orders reading the SKILL.md paths. *Fix:* commit symlinks `.agents/skills/<name>` → `../../.github/skills/<name>` (Codex explicitly follows symlinked skill dirs; frontmatter already agentskills-format) and thin .claude/commands/*.md wrappers ("Read and follow .github/prompts/<name>.prompt.md; $ARGUMENTS is the ${input} value") for **start-task** (see F3b — the most-invoked ritual), breakdown-epic, replan, distill-context, retro, onboard-project; optionally mirror the three .agent.md roles into .claude/agents/. Keeps .github/skills canonical, no content duplication. Two caveats: the .git/info/exclude blocker in F8 applies to the .claude/commands files too, and the distill-context wrapper makes that prompt's issue-less-branch defect (F12) discoverable before T-E0-1 fixes it — hence T-E0-7's interim brief line (§4). **[T-E0-0]**

**F5 — Onboarding not run; deterministic verification layer does not exist.** tuning-status.sh: NOT TUNED, 7 findings — 6 CUSTOMIZE markers plus the placeholder sentinel "Add project setup steps here" at copilot-setup-steps.yml:33, matched by the dedicated grep at tuning-status.sh:51 (verified by execution, exit 1). Consequences: copilot-instructions.md:29-32 tells sessions not to trust or extend the validated-commands section until onboarding runs (scoped to the commands section, not a blanket divert-to-onboarding directive) — which still blocks planners from writing grounded Verification sections, hollowing out verify-before-done; and the `quality` CI job always passes (`tuning-status.sh --ci` always exits 0 — scripts/tuning-status.sh:11,60-64), so Codex PRs get zero automated quality signal. Sub-decision needed: copilot-setup-steps.yml is 100% Copilot-cloud-specific and carries 2 of the 7 findings (one CUSTOMIZE marker at :22 + the sentinel at :33). Deleting it clears those 2 findings with no tuning-status.sh edit required — tuning-status.sh:45 (`[[ -f "$f" ]] || continue`) skips missing TARGETS files and the sentinel grep at :51 tolerates absence via add_hits' `|| true` (:39-42); TARGETS cleanup is optional hygiene — but TUNED still requires resolving the other 5 CUSTOMIZE markers (copilot-instructions.md:14,36; ci.yml:24; firmware.instructions.md:7,33) in the same onboarding task under either branch. The real cost of the deletion branch is the doc ripple: check-md-links dedupes extracted refs (awk '!seen[$0]++', check-md-links.sh:40-41), so deleting the workflow makes it fail with exactly ONE missing ref (`.github/workflows/copilot-setup-steps.yml`) — but clearing that one failure requires editing all three files carrying the REF_PATTERN-matching full path (copilot-instructions.md:38, verification/SKILL.md:59, project-onboarding/SKILL.md:17), two of which sit outside a naive T-E0-3 ownership set; and project-onboarding P5's definition of "tuned" (SKILL.md:88-90) requires copilot-setup-steps.yml to execute via workflow_dispatch, so deletion also means amending the onboarding skill's own definition of done. Keep-and-fill is materially cheaper (one placeholder step vs a five-file ripple — and the workflow_dispatch trigger P5 requires already exists at copilot-setup-steps.yml:8, verified, so "fill" means one real setup step, nothing more) — see §5 Q1. Security-toggle facts, now verified rather than deferred (fifth pass, `gh api repos/.../security_and_analysis`): secret scanning, push protection, non-provider patterns, and validity checks are all ENABLED; dependabot_security_updates is DISABLED; no dependency-review workflow exists — T-E0-3's confirmation step narrows to those two open items. *Fix:* run project-onboarding P0–P6 as its own Task issue with the scope carve-outs in §4 T-E0-3 (note the minor conflict: project-onboarding P6 mentions only a PR, no issue). **[E0-task]**

**F6 — Ruleset disabled, and misconfigured for a solo repo even if enabled.** `gh api repos/.../rulesets/19131818`: enforcement=disabled, rules: required_approving_review_count=1, **require_code_owner_review=false**, bypass_actors=[], required checks `quality`+`scaffold-self-check`. Disabled means direct pushes to main succeed (commit 35c1f6f went straight to main and broke CI). Enabled as-is means deadlock: GitHub forbids self-approval and both human and Codex operate as mochan-tk. And because require_code_owner_review is false (also hardcoded in setup-ruleset.sh's payload, ~:99), CODEOWNERS review stays unenforced even after enabling — "inert while disabled" understates it. The obvious fix (required_approving_review_count=0) creates a documented contradiction with SIX durable statements of the human-approval rule (grep-verified inventory; no further sites beyond advisory prompt lines): docs.instructions.md:22-23 ("at least one human approval" for docs/agreements changes), verification/SKILL.md:24-26 layer 4 ("human approval on agent-authored PRs"), docs/agreements/README.md:6-7 ("**PR + human approval only** — merge is what makes something an agreement"), the .github/CODEOWNERS header comment ("both need a human reviewer on every change", promising ruleset pairing), context-distillation/SKILL.md:34-35 ("Agreements change only via PR with human approval" — the operative step 4 of the very procedure T-E0-7 executes), and retro/SKILL.md:74-75 ("Human review approves — a retro PR changes how all agents behave" — the operative step 5 of the procedure T-E0-5 itself runs under) — count=0 makes all six dead letter unless either (a) a second account/bot becomes reviewer/code-owner (flip require_code_owner_review=true), or (b) those statements are amended to match reality (human merges = implicit approval). Execution note: setup-ruleset.sh only POSTs new rulesets (:~136) — rerunning it duplicates rather than updates; T-E0-3's step must PATCH ruleset 19131818 (or delete+recreate). Also `quality` is a guaranteed-green check until F5 lands. *Fix:* human decision (§5 Q3), then `gh api` PATCH: enforcement=active with the chosen review configuration, after F5 makes `quality` real; reconcile the six wording sites in the same wave — five inside T-E0-3's serialized ownership (verification/SKILL.md and context-distillation/SKILL.md serialized behind T-E0-1 via the existing blocked-by edge; retro/SKILL.md, README.md, CODEOWNERS in no other task's set), the docs.instructions.md:22-23 word-change riding T-E0-5's amendment PR to keep ownership disjoint (see §4). **[E0-task]** (execution is a gh-api acceptance step inside T-E0-3 — see §4)

**F7 — §8 English-only is contradicted by live artifacts.** AGENTS.md:64-68 covers "issues... and every Markdown file"; docs.instructions.md:9-11 and the PR-rejection list (copilot-instructions.md:86) reinforce it. Violations: all 9 epic titles/bodies in Japanese; the committed docs/context/plan-intake/ bundle in Japanese; the root README.md in Japanese (README.md:3-4 — outside docs/**, so a docs/context-scoped carve-out would NOT cover it); NG-007 contains an untranslated open item. *Why it matters:* either the constitution is dead letter from day one (corrosive to every other rule) or the epics are non-compliant. *Fix:* human choice (§5 Q2), then either the carve-out branch or translation. The carve-out branch must amend TWO rule sites, not one: AGENTS.md §8 (retro: PR) AND docs.instructions.md:9-11, whose `applyTo: "docs/**"` (:2) independently mandates "English only, for every file in this tree" — covering docs/context/ — so amending §8 alone merely relocates the contradiction into a path-scoped rule; copilot-instructions.md:86's rejection line may also need a word. Distillation outputs (REQ/NG/ADR) must be English either way. **[E0-task — rides T-E0-5, the consolidated constitution-amendments PR]**

**F8 — Claude Code permission friction, and the current config cannot ship — including a hidden per-clone blocker.** .claude/settings.local.json allowlists only `Bash(gh --version)` and `Bash(gh auth *)`, so every gh issue/pr/api call the §2/§3 protocol requires will prompt — and the file is NOT git-tracked. The mechanism matters: this clone's .git/info/exclude:7 contains the pattern `.claude/` (verified: `git check-ignore -v .claude/settings.local.json` → `.git/info/exclude:7:.claude/`; no global core.excludesfile is set), which is why `git status` reads clean. That same pattern silently blocks the fix itself: `git add .claude/settings.json` or `.claude/commands/*.md` (F4, F10's review-pr wrapper) will be refused as ignored or stay invisible in `git status` — and because worktrees share the main .git directory, every per-task worktree created from this clone inherits the exclusion. Fresh clones are unaffected (info/exclude is not cloned), but this clone and its worktrees are exactly where the flow runs. And the removal has a second-order effect the fix must absorb: once the exclude line is gone, the personal, never-to-be-committed .claude/settings.local.json surfaces as a `??` entry in `git status --short` in this clone AND every shared-.git worktree — but the child-session Verify step requires `git status --short` "(must be clean)" before every completion claim (session-orchestration/SKILL.md:49-51), and `git status` is the constitution's named §3 evidence command (AGENTS.md:30-33) — so without a .gitignore entry every future task session either fails/annotates its verify step forever or is nudged toward `git add -A`-committing a personal permission allowlist. Verified: the tracked .gitignore (13 lines: plan.md, OS cruft, editor swap patterns) has NO .claude entry. *Fix (three steps, one task):* T-E0-0's brief must FIRST remove the `.claude/` line from .git/info/exclude (a per-clone, unversioned file — safe to edit, no PR possible or needed); THEN add the line `.claude/settings.local.json` to the tracked .gitignore in the same PR (keeps the tracked/untracked split the fix itself proposes: settings.json shared, settings.local.json personal); THEN commit a shared `.claude/settings.json` containing the read-only gh allowlist (gh issue/pr/label/api-GET/project view verbs), leaving settings.local.json for personal extras. Acceptance checks: `git check-ignore .claude/settings.json` returns nothing AND `git status --short` is clean with settings.local.json present. PR-reviewable, survives clones and worktrees once tracked. **[T-E0-0]**

**F9 — MCP pointers are dead for both target tools.** .vscode/mcp.json holds only Playwright (confirmed) and is read by nothing in the target toolchain: Claude Code reads root .mcp.json — absent; Codex reads ~/.codex/config.toml only — per-project MCP config (which could eventually cover a repo-local mcp.json) is an open feature request, openai/codex#13056, and .vscode/mcp.json specifically is supported nowhere in the target toolchain. context-collection/SKILL.md:50-53 points agents at .vscode/mcp.json / cloud-agent repo settings. Scout-fact note: the claimed COPILOT_MCP_ prefix notes do NOT exist (case-sensitive grep over the repo: zero hits). No MCP is strictly required for the gh-driven flow. *Fix:* reword the skill line to "your client's MCP config (.mcp.json for Claude Code, ~/.codex/config.toml for Codex, .vscode/mcp.json for VS Code)"; fold into the F2 PR. **[E0-task]** (same PR as F2)

**F10 — Reviewer role is orphaned; verification layer 3 silently vanishes.** verification/SKILL.md:21-23 (layer 3, AI review) names "Copilot code review and/or the `reviewer` agent"; reviewer.agent.md defines the checklist but no surface in the Claude/Codex split runs it, and no review-pr prompt exists. With no CI ownership check (F6/R8) the File-ownership rule then rests on the human alone. *Fix:* one line in verification/SKILL.md re-hosting layer 3 on a Claude session reading .github/agents/reviewer.agent.md + code-review.instructions.md; add a thin .claude/commands/review-pr.md wrapper. **[E0-task]** (fold the skill line into F2's PR; the command file into F4's T-E0-0 batch)

**F11 — frontier.sh known limitations.** No type:task filter (any ai:ready open issue looks dispatchable — frontier.sh:65-66); output is number+title only, so the orchestrator needs an extra `gh issue view --json labels` round-trip per row to learn the surface (frontier.sh:65-68,89); JSON-mode cross-repo blockers mis-resolve (header admits it, :14-15). All low-impact in a single-repo project. *Fix:* optionally add `labels` to --json and a type:task filter when someone next touches the script. **[defer]**

**F12 — Prompt checklists diverge from the operative rules: board-sync missing, and distill-context scripts an issue-less PR path.** Part one — board sync (correction to an earlier draft claim): plan-management DOES cover post-creation board sync — the Roadmap scheduling section (plan-management/SKILL.md:105-113) explicitly says "Set an issue's schedule span when the Task is created during decomposition, and update it whenever replanning moves the schedule", with the exact command `scripts/setup-project.sh dates --project <n> --issue <n> --start … --target …`; and setup-project.sh's `dates` subcommand (cmd_dates, :189-289) scripts an idempotent `gh project item-add`, both date fields, and auto-sets Kind from type:epic/type:task labels. The actual residual gap is narrow: new-task.sh (:51-59) does not invoke it, and neither breakdown-epic.prompt.md step 5 (:22-26) nor replan.prompt.md repeats the step — so a session following only the prompt checklist will create tasks that never reach board #5, and the Roadmap (the human's window) silently diverges. Current gh auth has the `project` scope. Part two — worse, found in the fifth pass: distill-context.prompt.md:18 instructs "Open one PR on a branch `task/<issue-or-topic-slug>`" — explicitly permitting a TOPIC-slug branch with no issue number, i.e. an issue-less PR path that contradicts the iron rule this audit is defending (§4's `task/<issue-number>-<short-slug>` + `Closes #<n>`, AGENTS.md:36-41; "No issue, no work", session-orchestration/SKILL.md:79-84; PULL_REQUEST_TEMPLATE.md requires `Closes #<n>`). It sits on the critical path: T-E0-7 (the agreements distillation PR) is exactly the work this prompt scripts, and T-E0-0's .claude/commands/distill-context wrapper (F4) makes the flawed prompt discoverable to Claude Code sessions — a stateless session following wrapper+prompt would open a Closes-less PR on a numberless branch. No task in an earlier cut owned this file, so fixing it at PR time would have been an out-of-ownership diff (a PR-rejection trigger, copilot-instructions.md:80); it is now in T-E0-1's ownership and brief. *Fix:* reference the existing SKILL.md:105-113 step from the breakdown-epic/replan prompt checklists (one line each), or add an optional `--dates` passthrough to new-task.sh — do not re-document what the skill already mandates; AND rewrite distill-context.prompt.md step 5 to "Open one PR on branch `task/<issue-number>-<short-slug>` — distillation runs as its own Task issue (create it first if none exists); the PR body carries `Closes #<n>`". Because T-E0-7 (via T-E0-6←T-E0-0) is NOT serialized behind T-E0-1, T-E0-7's brief carries an interim guard restating the correct branch/Closes form (§4). **[E0-task]** (fold into F2's PR)

**F13 — Scoped `applyTo` instructions reach no target surface.** code-review/docs/firmware .instructions.md auto-inject only on Copilot; Claude and Codex never see them unless linked. Concretely: Codex could edit docs/agreements/ on a task branch without ever seeing the PR-only rule (docs.instructions.md:20-36); firmware.instructions.md's glob matches nothing yet (no firmware/ dir). *Fix:* planner rule (in planner.agent.md quality bar or breakdown prompt): every Task issue's Context & references must link the .instructions.md files whose globs cover its File-ownership paths. **[E0-task]** (fold into F2 PR)

**F14 — Automated issue/PR writers have no documented lane.** Two exist. (a) Dependabot: one PR ("Bump actions/checkout from 4 to 7", checks green but stale — see R8 — still open) unowned since 2026-07-18; violates "no issue, no work" by construction, with no documented exception. (b) retro-hygiene.yml (the repo's third workflow, omitted from earlier inventories): a monthly scheduled job (cron `0 0 1 * *`, workflow_dispatch fallback) running scripts/retro-hygiene.sh to auto-file an idempotent "Retro hygiene review <YYYY-MM>" issue labeled needs:human, reporting open retro:candidate promotion status and the ~150-line always-on budget (retro-hygiene.sh:44; AGENTS.md at 96 lines and copilot-instructions.md at 86 today — the budget table gives early warning if T-E0-1/T-E0-5 additions push toward the target). It is fully surface-neutral (pure gh + GITHUB_TOKEN, no Copilot dependency) — good news — but it will file its first needs:human issue on 2026-08-01, within a week of this audit, and nobody in the E0 cut owns triaging it. *Fix:* one or two sentences in copilot-instructions.md or plan-management: "dependabot PRs are exempt from issue-first; the human merges them directly. Retro-hygiene issues are needs:human by construction; the human triages them monthly." **[T-E0-3]** (pending human confirmation, §5 Q4)

**F15 — Bundle-internal loose ends for the distillation wave.** (a) REQ draft Source columns point at docs/context topic dirs that do not exist yet (docs/context/plan-intake/stackchan-plan/agreements-draft/requirements.md:8-14 — the comment at :8-10 states the design-sessions/2026-07-claude-planning/ dir is to be created in E0, and the table rows cite it; NOT to be confused with the scaffold's own docs/agreements/requirements.md example table) — intake must precede the distillation PR or every REQ cites a void. (b) The committed plan-intake bundle itself violates the repo's own intake rules: context-collection/SKILL.md:24-37 makes the provenance header (source/retrieved/method/collector/sensitivity/status) mandatory for every collected file and docs/context/README.md:10-15 requires per-topic INDEX.md, but docs/context/plan-intake/stackchan-plan/ has neither (verified: directory holds CODE-KICKOFF.md, HANDOFF.md, agreements-draft/, epics/, scripts/ — no INDEX.md; both md files open with plain headings). Distillation procedurally starts by reading INDEX.md (context-distillation/SKILL.md:26-27) and ends by flipping `status: raw` → `distilled` (:62-63) — impossible on header-less files. (c) NG-007 embeds an undecided distribution-method item inside a would-be agreement. (d) HANDOFF.md:56 declares the epic .md files obsolete post-filing — nobody should edit them instead of the issues. *Fix:* sequence E0 tasks intake→distill; T-E0-6 retrofits provenance headers + INDEX.md onto plan-intake/ (method: export, source: the Claude planning conversation); convert NG-007's open item into a tracked question. **[E0-task]**

**F16 — Claude Desktop, the planning surface E0 actually names, has no constitution delivery path.** E0's success criterion reads 設計=Claude Desktop(Fable 5)=planner/orchestrator (E0-scaffold-and-distill.md:8), while CODE-KICKOFF.md:1 is written "Claude Code用". The CLAUDE.md @import bridge the whole R5 story rests on (CLAUDE.md:1-5) is a Claude Code mechanism; Claude Desktop does not auto-load repo CLAUDE.md or AGENTS.md at all. So for one of the two named Claude surfaces there is NO documented delivery of the constitution, the skills, or the §9 start ritual — every Desktop planning session starts constitution-blind unless the human pastes files manually, exactly the improvised seam F3 describes on the other lanes. *Fix:* human decision folded into §5 Q6, executed in T-E0-1b's ADR + T-E0-2's dispatch doc: either (a) declare Claude Code the sole planning surface (Desktop allowed only via a Claude Project whose instructions embed or point at AGENTS.md), or (b) T-E0-2 adds a "Claude Desktop session bootstrap" snippet (paste AGENTS.md + issue number, or an MCP/gh-based loader). **[E0-task]**

*Reader/research disagreement log:* (1) The constitution-area reader treated Codex's pickup of AGENTS.md as an assumption; research confirms it as native, documented behavior (high confidence) — so R3/R6 are better than that reader feared, with the 32 KiB combined-AGENTS.md cap as the only new constraint (current AGENTS.md is well under it). (2) Scout fact "COPILOT_MCP_ prefix notes" was disproven by grep (F9). (3) Board #5 was initially reported unverified; the revision pass verified it live (all 9 Epics on the board with correct dates and Kind) — only the Roadmap view's existence remains API-invisible. An earlier characterization of CODE-KICKOFF.md as "claiming done" was wrong: it is the runbook that scheduled those steps, not a completion record. (4) An earlier claim that plan-management lacks board-sync coverage was wrong (see F12 correction). (5) A reviewer's claim that four md files reference the copilot-setup-steps.yml path was overcounted: three files carry REF_PATTERN-matching full paths (copilot-instructions.md:38, verification/SKILL.md:59, project-onboarding/SKILL.md:17); project-onboarding/SKILL.md:89 and SCAFFOLD-CHANGELOG.md:118 mention it only by bare name/prose, which check-md-links ignores. (6) A second review pass corrected: the "7 CUSTOMIZE sites" scout fact is really 6 CUSTOMIZE markers + 1 placeholder sentinel; check-md-links dedupes refs so the F5 deletion branch fails once, not three times; epic.yml's blurb is form-UI-only, never in the issue body; openai/codex#13056 requests per-project MCP config generally, not .vscode compatibility; verification layer 3 sits at SKILL.md:21; Dependabot has merged nothing (PR #1 still open); and earlier drafts had missed check-template-sync.sh, retro-hygiene.yml, the shellcheck raw-context exposure, and four smaller Copilot-wording sites — all incorporated above. (7) A third review pass corrected: an earlier draft claimed create-epics.sh passes shellcheck "only because the run dies at the later md-links step" — false on both mechanism and fact (shellcheck runs BEFORE md-links, ci.yml:61-62 vs :78-79, and the file passes on merit with the CI-pinned 0.11.0; the md-links exclusion alone restores green CI); the same pass found the .git/info/exclude `.claude/` blocker (F8), the unguarded label-set drift (F2), the missing Claude→Claude dispatch lane (F3b), and consolidated research citations on the developers.openai.com/codex docs host. (8) A fourth review pass corrected: developers.openai.com/codex URLs 308-redirect to learn.chatgpt.com (verified live: /codex/skills → learn.chatgpt.com/docs/build-skills), so the two hosts serve the same pages and the earlier learn.chatgpt.com citations were never miscitations; the third pass's "both ALLOW_PREFIXES entries are dead config, delete them in T-E0-0" claim was wrong and would have re-broken CI on T-E0-0's own PR — the device-pairing entry is load-bearing (masks 2 refs in docs/agreements files that stay scanned; see F1 correction) and its removal now rides T-E0-7; ai-task.yml is NOT change-free (a grep for "Copilot" had tested the wrong string — the fourth "(exec:cloud tasks get no hardware)" copy sits at ai-task.yml:74-76, now in T-E0-1's scope); the F2 inventory had omitted its two highest-visibility sites (the AGENTS.md:3-5 preamble and CLAUDE.md:1-3 comment, now scheduled in T-E0-5); F3b's "nothing says who creates the per-task worktree" was overstated (start-task.prompt.md:18 says the child does, in an undiscoverable prompt); T-E0-1's plan-management "echo sentence" would have contradicted unamended §4 during the window between merges (dropped — AGENTS.md:10's conflict clause, the draft's own argument); and T-E0-1/T-E0-5 lacked the blocked-by edges to T-E0-0 that the cut's own F12-anti-pattern argument demands (added). (9) A fifth review pass corrected: "deletion alone yields TUNED" was literally false (deletion clears only copilot-setup-steps.yml's 2 of 7 findings; the other 5 CUSTOMIZE markers still block TUNED — R7/F5/Q1 reworded); F1's HANDOFF.md:56 citation had overgeneralized "epics/ md obsolete" to "the bundle" (F15d had it right); the human-approval inventory was undercounted at four — grep found six, adding context-distillation/SKILL.md:34-35 and retro/SKILL.md:74-75 (F6/Q3, both now assigned); distill-context.prompt.md:18's issue-less branch instruction had been missed by every inventory and every ownership set (now F12 part two, fixed in T-E0-1 with an interim guard in T-E0-7); the F8 fix was self-defeating without a .gitignore line for settings.local.json (added, with the session-orchestration:49-51 clean-status argument); "the one deliberate re-ownership" undercounted its own exceptions (four serialized overlaps now enumerated in §4); T-E0-1's post-merge label rerun could not have had its evidence in the §2 Outcome comment (mechanism respecified as a recorded follow-up + post-close comment); "blocked by the Q_/Q_ decisions" had no mechanism (replaced with the scaffold's own needs:human / withheld-ai:ready gate); and secret-scanning status was verified rather than deferred (enabled incl. push protection; dependabot security updates disabled; workflow_dispatch already present at copilot-setup-steps.yml:8).

## 4. Proposed E0 task cut

Consistent with E0's phase outline (E0-scaffold-and-distill.md:19-26); all created via new-task.sh under Epic #2. Exec labels: the target semantics define no slug for Claude-executed work, so the T-E0-1 matrix rewrite (and Q5) must fix the Claude lane explicitly — proposed mapping: exec:cli = "Claude Code session (planning/docs/scripted repo work)", exec:app = "Codex desktop implementation session"; under that mapping every task below is exec:cli. (These labels are applied before T-E0-1 merges — acceptable exactly because slugs are being reinterpreted, not renamed; the Q5 answer, not a guess, authorizes it. Escape hatch if Q5 stalls: see Q5.) File ownership is stated as explicit file paths, not directories, so the sets stay provably disjoint among concurrently-runnable tasks — ci.yml is owned by exactly ONE task (T-E0-3) and docs.instructions.md by exactly one (T-E0-5), per §5's mandatory disjoint-or-serialized rule (AGENTS.md:44-49, plan-management/SKILL.md:38-40; hunk-level "line only" annotations have no enforcement meaning under a path-based single-writer rule). The deliberate re-ownerships are exactly FOUR, each legal under §5 because the pair is strictly serialized by blocked-by edges: scripts/check-md-links.sh (T-E0-0∩T-E0-7, via the T-E0-0→T-E0-6→T-E0-7 chain), verification/SKILL.md (T-E0-1∩T-E0-3, via T-E0-1→T-E0-3), context-distillation/SKILL.md (T-E0-1∩T-E0-3, same edge), and session-orchestration/SKILL.md (T-E0-1∩T-E0-2, via T-E0-1→T-E0-2). Ordering is encoded in the graph, not prose: T-E0-1, T-E0-5, and T-E0-6 all carry a blocked-by edge to T-E0-0 (`-d <T-E0-0 number>` at creation) — without it, frontier semantics would legitimately dispatch T-E0-1/T-E0-5 before the CI unbreak merges and their PRs would open with the inherited red check, the exact "verify-before-done becomes noise" condition F1 opens with; two of these edges cover the whole cut transitively (T-E0-1b/2/3/9 hang behind T-E0-1; T-E0-7/8 behind T-E0-6). Human-decision gating uses the scaffold's own mechanism, not prose (blocked-by edges can reference only issues — new-task.sh -d takes issue numbers, and frontier.sh gates on ai:ready): decision-gated tasks — T-E0-1 (Q5), T-E0-1b (Q5/Q6), T-E0-5 (Q2/Q3) — are created via new-task.sh WITHOUT `--ready` (new-task.sh:15,46 — ai:ready is opt-in) and then labeled `gh issue edit <n> --add-label needs:human` (canonical label, setup-labels.sh:21 "Escalation: judgment/trust decision required"), with the relevant §5 questions posted as a comment on the issue; when the human answers, swap needs:human for ai:ready — frontier.sh already excludes non-ai:ready issues, zero new machinery, and the decision-pending state is durable on GitHub per §1. Board dates for each task set via `setup-project.sh dates` at creation, per plan-management/SKILL.md:105-113.

0. **T-E0-0: Repo hygiene batch (FIRST task issue created)** — The former "[do-now]" items, run through the process rather than around it: with ruleset 19131818 disabled these would otherwise land as direct pushes to main — the exact pattern (commit 35c1f6f) F6 criticizes — so the reformed process's first act should be an instance of the process; the whole batch is a 10-minute issue+PR. Contents, in order: (pre-step, per F8) remove the `.claude/` line from .git/info/exclude — per-clone unversioned file, no PR possible or needed; (per F8) add the line `.claude/settings.local.json` to the tracked .gitignore in the same PR, so the exclude removal does not expose the personal settings file as permanent `??` noise in the `git status --short` cleanliness check every child session runs (session-orchestration/SKILL.md:49-51) — acceptance: `git check-ignore .claude/settings.json` returns nothing AND `git status --short` clean with settings.local.json present; F1's one-line check-md-links scan-scope exclusion (`:!docs/context/**`) PLUS deletion of ONLY the `docs/context/line-integration/` ALLOW_PREFIXES entry in the same one-file diff — the `docs/context/device-pairing/` entry is explicitly KEPT: it masks 2 live refs in docs/agreements/requirements.md:10 and non-goals.md:7 that stay scanned after the exclusion, and removing it re-breaks the check this task exists to fix (its removal rides T-E0-7, which owns the files containing those refs — see F1); HANDOFF.md itself stays untouched; F4's .agents/skills symlinks + .claude/commands wrappers — start-task, breakdown-epic, replan, distill-context, retro, onboard-project, plus F10's review-pr (+ optional .claude/agents mirrors); F8's committed .claude/settings.json. NOT in the batch: the ci.yml shellcheck scan-scope decision (ci.yml is T-E0-3's ownership — the interim guard is one line in T-E0-6's brief, see below) and F14's exemption sentence (copilot-instructions.md is also T-E0-3's). No blocked-by edges; a purely additive task whose ownership set no concurrent task shares. *Ownership:* scripts/check-md-links.sh, .gitignore, .agents/**, .claude/settings.json, .claude/commands/*.md, .claude/agents/*.md (covers the optional role mirrors — copilot-instructions.md's rejection rule triggers on any diff outside the declared set). (Implements F1, F4, F8.)
1. **T-E0-1: Reinterpret exec:* routing for the Claude/Codex split** — Rewrite the task-routing Surface matrix AND frontmatter description (keep slugs; add the explicit Claude lane per Q5); fix plan-management/SKILL.md:70's example label; update templates/task-body.md:44,55, verification/SKILL.md's routed-environment rule (:70-71), AND ai-task.yml's Verification-field description (:74-76) — all four copies of the "(exec:cloud tasks get no hardware)" vocabulary — to the new surface wording, and drop or annotate ai-task.yml's exec:cloud dropdown option (:87) as reserved/unused (constraint: scripts/check-template-sync.sh — run by scaffold-self-check, ci.yml:75-76 — keeps issue-form `label:` values in ordered sync with body-template `## ` headings, so any form-label change requires the paired template heading change in the same PR; the edits here touch comments/list text, `description:` and `options:` values, not `label:` keys or headings, and the epic.yml blurb is a `type: markdown` value block, so all are sync-safe — state this in the brief); rewrite the three Copilot-worded description strings in scripts/setup-labels.sh — with the label-propagating rerun handled §2/§3-compliantly: the rerun CANNOT have its evidence in this session's Outcome comment, because that comment lands at session end, before the human merges the PR; instead the Outcome comment records "rerun scripts/setup-labels.sh after merge" in its follow-ups field (session-orchestration's Outcome format has one), and after merge the human or orchestrator runs `bash scripts/setup-labels.sh` and appends the `gh label list` output as a NEW comment on the (closed) Task issue — issues stay writable after close, so record-before-report is satisfied by whoever executes the step; the merged-truth ordering also rules out mid-session `gh label edit` (silently reverted by any future rerun of the idempotent script — setup-labels.sh:4,16; project-onboarding P4 conditionally reruns it, SKILL.md:80) — optionally pruning the 9 unused GitHub default labels at the same rerun, per F2; reword orchestrator.agent.md description + Dispatch step, the "Copilot client version" header comments in all three .agent.md files, epic.yml form blurb, code-review.instructions.md:7; reword verification layer-3 (SKILL.md:21-23) and the CI-triage line (verification/SKILL.md:59, which names .github/workflows/copilot-setup-steps.yml as the usual env-failure suspect — reword contingent on the Q1 outcome); context-collection MCP pointer (:50-53); extend context-distillation's tier-table Always-on destination row (SKILL.md:44-46, currently listing only "AGENTS.md, .github/copilot-instructions.md") to name the actual delivery chain (CLAUDE.md @import for Claude Code, native AGENTS.md for Codex); planner rule for linking scoped instructions (F13); add the board-dates line to the breakdown-epic/replan prompt checklists (F12 part one); fix distill-context.prompt.md:18's issue-less branch instruction per F12 part two ("Open one PR on branch `task/<issue-number>-<short-slug>` — distillation runs as its own Task issue (create it first if none exists); the PR body carries `Closes #<n>`"). NOT in this task: any AGENTS.md or CLAUDE.md wording (constitution preamble and shim comment ride T-E0-5); the context-distillation:34-35 human-approval rewording (a Q3(a) consequence — rides T-E0-3, which executes the Q3 decision, serialized behind this task; mirrors the verification/SKILL.md:24-26 treatment); and NO plan-management "plan-only tasks close without a PR" sentence — that sentence, merged before T-E0-5 amends §4, would directly contradict AGENTS.md:36's "1 Task issue = … = 1 pull request" and trip AGENTS.md:10's conflict clause (stop-and-escalate) in every intervening session; §9 makes every session read AGENTS.md before any skill, so T-E0-5's §4 clause alone reaches every future plan-only session and no skill echo is needed. Blocked by T-E0-0; gated on Q5 via the needs:human mechanism in the preamble; blocks T-E0-1b/2/3 (not T-E0-6). *Ownership (explicit files):* .github/skills/task-routing/SKILL.md, plan-management/SKILL.md, plan-management/templates/task-body.md, plan-management/templates/epic-body.md (if headings move), session-orchestration/SKILL.md (frontmatter), verification/SKILL.md, context-collection/SKILL.md, context-distillation/SKILL.md; .github/prompts/breakdown-epic.prompt.md, .github/prompts/replan.prompt.md, .github/prompts/distill-context.prompt.md; .github/agents/planner.agent.md, orchestrator.agent.md, reviewer.agent.md; .github/instructions/code-review.instructions.md; .github/ISSUE_TEMPLATE/epic.yml, .github/ISSUE_TEMPLATE/ai-task.yml; scripts/setup-labels.sh. NOTE: ci.yml's cosmetic header reword (F2g) moves to T-E0-3, the sole ci.yml owner. (Implements F2 except its constitution sites, F9, F10-line, F12 both parts, F13. ai-task.yml's dropdown needs no slug changes — reinterpretation keeps the option values — but its :74-76 description and exec:cloud option are in scope here.)
1b. **T-E0-1b: Division-of-labor + dispatch-seam ADR** — Its own Task issue, NOT a second PR from T-E0-1's session (two PRs from one session would violate §4; docs.instructions.md:21-25 requires agreements changes to be a separate PR — a separate task satisfies both plus the single-writer rule). Records: the exec:* reinterpretation (Q5), the Claude-surface decision (Q6), the §4 branch-name widening chosen in T-E0-5 (F3/R3), and a one-line board Status-field convention. ADR ID: **ADR-0001** — the next free ID, per the operative path-scoped rule "ADRs are numbered sequentially from ADR-0001" (docs.instructions.md:31-32). The ADR-0001..0005 topic pre-allocation in E0-scaffold-and-distill.md:11,25 is raw context inside the epics/ payload HANDOFF.md:56 itself declares obsolete post-filing, and Epic #2's checklist is exactly what §7 rolling-wave expects to revise: T-E0-8's five architecture ADRs shift to ADR-0002..0006, recorded as a one-line rationale comment on Epic #2 at T-E0-1b creation time (the mapping was topic-to-count, not topic-to-sacred-number; reserving a gap at 0001..0005 would instead invite a literal reviewer to flag the T-E0-1b PR against docs.instructions.md on the very tree where that rule auto-applies). Blocked by T-E0-1 (issue edge); the Q5/Q6 gate is the needs:human / withheld-ai:ready state described in the preamble — blocked-by cannot reference a decision. *Ownership:* docs/agreements/adr/ADR-0001-division-of-labor.md (single file — disjoint from T-E0-7/8 at file level, so no extra blocked-by edges needed). (Implements F2-ADR, F16-ADR.)
2. **T-E0-2: Document the cross-tool dispatch protocol — all three lanes** — Add the surface-agnostic "Cross-tool dispatch" section per F3: **exec:cli lane** (fresh Claude Code session per task; worktree creation command `git worktree add ../wt-task-<n> -b task/<n>-<slug>` — promoting into a discoverable skill what today exists only inside start-task.prompt.md:18; start ritual invoked via the T-E0-0 start-task wrapper; parent↔child messaging = issue comments); **exec:app lane** (Codex kickoff snippet — including the explicit "create/rename your branch to task/<n>-<slug>" line, belt-and-braces alongside T-E0-5's §4 widening — issue-comment steering, gh-auth/network prerequisites, Codex worktree note); **Claude Desktop bootstrap** per the Q6 decision; update session-orchestration's frontmatter description if not done in T-E0-1. Blocked by T-E0-1 and T-E0-1b. *Verification:* the session-verifiable criteria are doc lands, links resolve, check-md-links green; the end-to-end Codex probe (kickoff snippet run in a real Codex thread, evidence = resulting issue-comment URL) is explicitly HUMAN-ASSISTED in the task brief — a Claude session cannot drive the Codex app itself. *Ownership:* .github/skills/session-orchestration/SKILL.md, .github/prompts/start-task.prompt.md (optional note). (Implements F3, F16-mechanics.)
3. **T-E0-3: Run project-onboarding (P0–P6), with explicit scope carve-outs** — Fill/resolve all 7 tuning findings, establish validated commands, make the `quality` CI job real, execute the copilot-setup-steps.yml decision, own ALL ci.yml edits as the file's sole owner (the CUSTOMIZE resolution at :24, the F2g header-comment reword, and the F1 scanner-consistency decision for the shellcheck file list — exclude docs/context/** or keep it covered), add the F14 dependabot/retro-hygiene lane sentences, decide the two verified-open security toggles (enable dependabot security updates? add a dependency-review workflow or document why not — secret scanning and push protection are already enabled, verified via `gh api .../security_and_analysis`), and finish with the ruleset enablement as a gh-api acceptance step: PATCH ruleset 19131818 (setup-ruleset.sh only POSTs — rerunning duplicates) to enforcement=active with the Q3-decided review configuration, verifying required check contexts match ci.yml job ids; under Q3(a) reconcile FIVE of the six human-approval wording sites here (verification/SKILL.md:24-26 and context-distillation/SKILL.md:34-35 — both serialized behind T-E0-1's ownership of those files via the existing blocked-by edge — retro/SKILL.md:74-75, docs/agreements/README.md:6-7, the CODEOWNERS header comment); the sixth, docs.instructions.md:22-23, rides T-E0-5's amendment PR so the two tasks never share a file (see T-E0-5). Pre-declared deviation line for the brief (a strict reading of docs.instructions.md:20-25 forbids agreements edits riding a non-agreements PR — the same reading this cut uses to justify T-E0-1b): "the retro-log edits are mandated by project-onboarding P6, the skill IS the sanctioned procedure; the docs/agreements/README.md:6-7 rewording executes the Q3 human decision and the human reviews it in this PR" — declared at creation time, not discovered at PR time. Retro-log note for the brief: when P6 appends this onboarding's row to docs/agreements/retro-log.md, also fix the six IMPORTED scaffold rows (retro-log.md:9-14) whose Evidence column cites bare issue refs (#7 #9 #10 #11 #13 #18 #34 #36) from the source scaffold repo — rendered here they auto-link to this repo's unrelated live issues (#7 = Epic E5, #9 = Epic E7) or to nothing; prefix them with a one-line provenance note or qualify the refs (e.g. `mrmo-sandbox/<scaffold-repo>#7`) so the ledger the monthly retro-hygiene issues point humans at is not misattributed. Scope carve-outs the brief must state (single-writer rule): P4's "seed documents into docs/context/" is delegated to T-E0-6 — skip it here; P6's retro-log row → add docs/agreements/retro-log.md to this task's ownership (serialized behind T-E0-1 via blocked-by; T-E0-7/8 come later). If the deletion branch of Q1 is chosen, ownership must additionally cover the three files whose full-path refs feed the (deduped) missing-ref failure (copilot-instructions.md:38 is already owned; verification/SKILL.md:59 is serialized behind T-E0-1; project-onboarding/SKILL.md:17 must be added) plus project-onboarding P5's "tuned" definition (SKILL.md:88-90) — listed as explicit acceptance criteria; keep-and-fill avoids the ripple entirely. Blocked by T-E0-1. *Ownership:* .github/copilot-instructions.md, .github/workflows/ci.yml (sole owner), .github/workflows/copilot-setup-steps.yml, .github/instructions/firmware.instructions.md, docs/agreements/retro-log.md, verification/SKILL.md + context-distillation/SKILL.md + .github/skills/retro/SKILL.md + docs/agreements/README.md + .github/CODEOWNERS (Q3(a) reconciliation, as decided) (+ scripts/tuning-status.sh as optional hygiene; + scripts/setup-ruleset.sh if PATCH support is added to it; + project-onboarding/SKILL.md only under Q1=delete). (Implements F5, F6, F14, the F1 shellcheck-scope decision, and the two open security-toggle decisions.)
4. **T-E0-4 — MERGED INTO T-E0-3.** Ruleset enablement produces no file diff, so a standalone task cannot satisfy §4's 1-task-1-branch-1-PR completion protocol; it rides T-E0-3 as an acceptance step (matching F6). If setup-ruleset.sh itself is improved (PATCH support), that diff rides the onboarding PR (and the file is in T-E0-3's ownership for exactly that case).
5. **T-E0-5: Constitution amendments retro PR (broadened from §8-only)** — All AGENTS.md edits consolidate here, because AGENTS.md changes only via PR to itself (Amendments section, AGENTS.md:91-96) and its conflict clause (AGENTS.md:10: "If any instruction conflicts with this file, stop and escalate per §6") means a skill sentence contradicting a §-numbered rule triggers a designed-in escalation — an ADR cannot amend the constitution, and a plan-management-only exception loses to §4 on contact. Four surgical edits: (1) the §8 carve-out per Q2 (docs/context raw material, issue bodies/titles, root README — or nothing, if Q2=translate); (2) a half-line widening of §4's accepted-equivalent branch wording to cover number-less tool-generated names when the PR body carries `Closes #<n>` (removes F3's dependence on the kickoff-snippet rename instruction; the snippet stays as belt-and-braces); (3) a half-line §4 clause "planning-only tasks close with the recorded plan on the Epic; no PR" (grounds T-E0-9); (4) reword the preamble's fleet enumeration (AGENTS.md:3-5, currently "every surface: GitHub Copilot cloud (coding) agent, GitHub Copilot app sessions, Copilot CLI, IDE chat agents, and third-party agents") to the actual Claude/Codex fleet — the highest-visibility stale-routing text in the repo, since AGENTS.md is the only file Codex auto-loads (F2i); budget-neutral, a rewording not an addition. Budget rule note for the brief: ~2 net lines added to AGENTS.md (96 lines today against the ~150 target, retro-hygiene.sh:44) — no removal strictly required, but say so explicitly. Companion edits in the same PR: the CLAUDE.md:1-3 comment's "identically to the Copilot surfaces" phrase (one-word class of F2i; the @import lines themselves are unchanged); .github/instructions/docs.instructions.md — the Q2 carve-out mirror at :9-11 (its docs/** scope independently mandates English, F7) AND the Q3(a) "at least one human approval" rewording at :22-23 (moved here from T-E0-3 so the two tasks never co-own the file; language-policy-adjacent, coheres). copilot-instructions.md:86's rejection-list word stays OUT: copilot-instructions.md is T-E0-3's; if Q2's carve-out requires touching that line, serialize via a T-E0-3→T-E0-5 blocked-by edge or hand the one-word edit to T-E0-3's brief — state the choice at creation time, do not leave it to PR-time discovery. Blocked by T-E0-0 (inherited-red-CI edge); gated on Q2/Q3 via the needs:human mechanism in the preamble — blocked-by cannot reference a decision. *Ownership:* AGENTS.md, CLAUDE.md, .github/instructions/docs.instructions.md (+ README.md if Q2=translate). (Implements F7 and F2's constitution sites; grounds F3's branch-name fix and T-E0-9's closure rule.)
6. **T-E0-6: Context intake** — Create docs/context/{design-sessions/2026-07-claude-planning,azure-foundry,line-miniapp,stackchan-hw}/ with provenance headers + INDEX.md, including the Claude planning-conversation logs (HANDOFF.md:55); retrofit provenance headers + an INDEX.md onto docs/context/plan-intake/stackchan-plan/* (F15b). Brief must state: until T-E0-3 lands the shellcheck-scope decision, committed raw-context .sh files must pass `shellcheck -S style` (the shellcheck step scans all tracked .sh and is NOT excluded by T-E0-0's md-links fix). NOT blocked by T-E0-1 (no ownership overlap; keeping the frontier stocked is the point of JIT decomposition, plan-management/SKILL.md:32-33) — but **explicitly blocked by T-E0-0** (`-d <T-E0-0 number>` at creation): committing conversation logs before the check-md-links exclusion merges re-fires exactly the F1 failure class this audit opens with, and plan-management/SKILL.md:38-40's overlap rule states when an edge is REQUIRED, not the only case where one is permitted — blocked-by is the generic ordering mechanism frontier.sh gates on, and this cut already uses pure-sequencing edges (T-E0-7←T-E0-6, T-E0-8←T-E0-7, and now T-E0-1/T-E0-5←T-E0-0); a known precondition left as prose would repeat the F12 anti-pattern. *Ownership:* docs/context/** only. (Implements F15a/b; E0 phase 3.)
7. **T-E0-7: Agreements distillation PR** — REQ-001..019 + NG-001..008 in English into docs/agreements/, provisional markers on REQ-003/REQ-017, NG-007's open item converted to a tracked question. Also in this PR (per F1): replacing the "(example — replace)" rows removes the last references into docs/context/device-pairing/ (requirements.md:10, non-goals.md:7, glossary.md:8), so delete the now-truly-dead `docs/context/device-pairing/` ALLOW_PREFIXES entry from scripts/check-md-links.sh:27 in the same diff — acceptance criterion: `bash scripts/check-md-links.sh` green with 0 allowlisted refs skipped. Interim guard in the brief (F12 part two): this task's branch is `task/<issue-number>-<short-slug>` and its PR body carries `Closes #<n>` — restated explicitly because distill-context.prompt.md:18 still permits an issue-less topic-slug branch until T-E0-1's fix merges, and this task is NOT serialized behind T-E0-1. Blocked by T-E0-6. *Ownership (explicit files):* docs/agreements/requirements.md, non-goals.md, glossary.md, scripts/check-md-links.sh — the last overlaps T-E0-0's set but is legal under §5 because T-E0-7 is strictly serialized behind T-E0-0 via the T-E0-0→T-E0-6→T-E0-7 blocked-by chain; disjoint from T-E0-1b's ADR-0001 and T-E0-8's ADR-0002..0006. (E0 phase 4; F15c; F1 allowlist retirement.)
8. **T-E0-8: ADR-0002..0006 + protocol v0 spec** — Architecture, device-proxy protocol v0 (spec file referenceable from E2/E3), audio pipeline, auth, client strategy — the five topics from E0-scaffold-and-distill.md:11,25, renumbered from 0001..0005 per T-E0-1b's rationale comment on Epic #2; the brief lists each topic with its new ID so the defined mapping survives intact. Blocked by T-E0-7. *Ownership:* docs/agreements/adr/ADR-0002..ADR-0006 (+ the protocol spec file). (E0 phase 5.)
9. **T-E0-9: E1 breakdown** — Decompose E1 into ai:ready sub-issues using the now-corrected routing; add items to board #5 with dates via setup-project.sh. Blocked by T-E0-1..3, T-E0-5 (its §4 plan-only clause must exist before this task closes without a PR), T-E0-8. *Deliverable defined in the brief:* Epic rationale comment + list of created sub-issue numbers; no PR — legitimate under T-E0-5's §4 amendment alone: §9's start ritual makes every future plan-only session read AGENTS.md before any skill, so the exception lives where it is guaranteed to be read (a skill-level echo was deliberately dropped from T-E0-1 — merged early it would contradict unamended §4 and trip AGENTS.md:10's conflict clause; the T-E0-1b ADR may still cite the amended clause). (E0 phase 6.)

If the human prefers to treat F1's one-line CI unbreak as an emergency direct fix before T-E0-0 exists, that is the only item that plausibly merits it — and with T-E0-1/T-E0-5/T-E0-6 now formally blocked behind T-E0-0, an early T-E0-0 (or that one emergency fix) is also what unblocks the entire graph; everything else in T-E0-0 waits the extra ten minutes without cost.

## 5. Open questions for the human

1. **copilot-setup-steps.yml fate:** keep+fill it as cheap insurance for a future Copilot-cloud option (recommended: one placeholder step to fill — the workflow_dispatch trigger P5's "tuned" definition requires already exists at copilot-setup-steps.yml:8 — no ripple), or delete it? Deletion clears the file's 2 tuning findings with no tuning-status.sh edit (tuning-status.sh:45 skips missing TARGETS files) — the other 5 CUSTOMIZE markers still need resolving before TUNED under either branch — but it makes check-md-links fail with one deduped missing ref, clearable only by editing all three referencing files (two outside a naive T-E0-3 ownership set), and invalidates project-onboarding P5's "tuned" definition — a five-file ripple across two other tasks' ownership.
2. **§8 language policy:** amend the constitution to allow Japanese in docs/context raw material, GitHub issue bodies/titles, and the root README (agreements/code/commits/PRs stay English) — a two-site amendment, AGENTS.md §8 plus docs.instructions.md's docs/** English rule, both riding the broadened T-E0-5 constitution-amendments PR — or translate epics #2–#10 + README? Current state is a live contradiction either way.
3. **Ruleset review requirement:** with one human account and agents pushing as the same account, self-approval is impossible. Options: (a) required_approving_review_count=0 — but then amend all SIX human-approval statements (docs.instructions.md:22-23 — rides T-E0-5; verification/SKILL.md:24-26, context-distillation/SKILL.md:34-35, retro/SKILL.md:74-75, docs/agreements/README.md:6-7, the CODEOWNERS header comment — ride T-E0-3), whose "at least one human approval" wording becomes dead letter (human merges = implicit approval); or (b) second account/bot as reviewer AND code owner (also flip require_code_owner_review, currently false both live and in setup-ruleset.sh). Note CODEOWNERS is unenforced under (a) even with the ruleset active.
4. **Automated-writer lanes:** confirm the exemptions "dependabot PRs bypass issue-first; human merges directly" and "monthly retro-hygiene issues (first one lands 2026-08-01) are triaged by the human" — or should dependabot PRs get retroactive Task issues?
5. **exec label vocabulary — two decisions:** (a) confirm reinterpretation of existing slugs over introducing new labels (reinterpretation touches the routing skill + setup-labels.sh descriptions + templates + the ai-task.yml description + a few prose spots; renaming touches new-task.sh whitelist, templates, the form dropdown options, labels, and skill text); (b) confirm the Claude lane, which the target semantics leave undefined although every Task must carry exactly one exec:* label: proposed exec:cli = "Claude Code session (planning/docs/scripted repo work)" alongside exec:app = "Codex desktop implementation session" — this labels all nine §4 tasks exec:cli and removes the guess the Ambiguity rule forbids. **Escape hatch so the CI unbreak never waits on this decision:** if Q5 is unanswered when T-E0-0 is cut, create it with exec:ide — its live description "Route: IDE with human in the loop" (setup-labels.sh:26) is already tool-neutral and accurate for a human-supervised Claude Code session — and relabel to the Q5 vocabulary afterwards (`gh issue edit` is cheap; §7 makes plan revision normal). The one-line main-CI fix must not have a label-semantics question on its critical path.
6. **Claude planning surface + delivery:** E0 names "Claude Desktop (Fable 5)" while CODE-KICKOFF is "Claude Code用" — and Claude Desktop auto-loads NO repo files (F16). Decide both naming and mechanics: (a) Claude Code as sole planning surface (Desktop only via a Claude Project embedding AGENTS.md), or (b) keep Desktop and add a documented Desktop bootstrap (paste-in snippet or loader) in T-E0-2. The T-E0-1b ADR records whichever is chosen.
7. **NG-007 distribution method** (TestFlight/private vs other): flagged in the draft itself as "decide before E5" — does it stay an open question inside the accepted non-goal, or become its own tracked issue at distillation time?
